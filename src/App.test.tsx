import{render,screen}from'@testing-library/react';import{describe,expect,it,vi}from'vitest';import{axe}from'vitest-axe';import App from'./App';
vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify({items:[],next_page_params:null}))));
describe('App',()=>{it('renders the pinned board and search without axe violations',async()=>{location.hash='#/';const{container}=render(<App/>);expect(screen.getByRole('heading',{name:'IMD dev board - read via IDM Inbox (independent)'})).toBeInTheDocument();expect(screen.getByLabelText('Open an address or ENS name')).toBeInTheDocument();expect(await screen.findByText('0 threads loaded')).toBeInTheDocument();expect((await axe(container)).violations).toEqual([])});it('shows not found',()=>{location.hash='#/wrong';render(<App/>);expect(screen.getByRole('heading',{name:'Not found'})).toBeInTheDocument()})});
